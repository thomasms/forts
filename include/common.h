#ifndef COMMON_HH
#define COMMON_HH

#if !defined(NAMESPACE_START)
    #define NAMESPACE_START(name) namespace name {
#endif

#if !defined(NAMESPACE_USING)
    #define NAMESPACE_END }
#endif

#if !defined(NAMESPACE_USING)
    #define NAMESPACE_USING(name) using namespace name;
#endif

#endif // COMMON_HH
